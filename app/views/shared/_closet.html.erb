<div class="container-fluid">
    <div class="row main">
        <div class="col s12 m4 sidebar">
            <div class="row">
                <div class="clothing-type-title">
                    <p>shirts</p>
                </div>
                <div class="divider"></div>
                <div class="horizontal">
                  <a href="http://localhost:3000/items/new" class="btn-floating fab btn-xs waves-effect waves-light right-align green"><i class="material-icons">add</i></a>

                    <div class="clothing-type">

                      <% @items.each do |item| %>
                      <div class="col s12 m4">
                          <div class="item">
                            <%= image_tag item.image.url(:medium) %><br>
                            <%= link_to "Edit", controller: "items", action: "edit", id: item.id %>
                          </div>
                      </div>
                      <% end %>
                    </div>
                </div>
            </div>
            <div class="row">
                <div class="clothing-type-title">
                    <p>pants</p>
                </div>
                <div class="divider"></div>
                <div class="horizontal">
                    <div class="clothing-type">
                      <% @items.each do |item| %>
                      <div class="col s12 m4">
                          <div class="item">
                            <%= image_tag item.image.url(:medium) %><br>
                            <%= link_to "Edit", controller: "items", action: "edit", id: item.id %>
                          </div>
                      </div>
                      <% end %>
                    </div>
                </div>
            </div>
            <div class="row">
                <div class="clothing-type-title">
                    <p>shoes</p>
                </div>
                <div class="divider"></div>
                <div class="horizontal">
                    <div class="clothing-type">
                      <% @items.each do |item| %>
                      <div class="col s12 m4">
                          <div class="item">
                            <%= image_tag item.image.url(:medium) %><br>
                            <%= link_to "Edit", controller: "items", action: "edit", id: item.id %>
                          </div>
                      </div>
                      <% end %>
                    </div>
                </div>

            </div>
        </div>
