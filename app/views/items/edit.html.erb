<%= render "shared/header" %>

<h2>Edit Item</h2>

<%= form_for @item do |f| %>
  <label> Name </label> <br>
  <%= f.text_field :name, value: @item.name, placeholder: "Enter new Name" %><br>
  <label> Brand </label><br>
  <%= f.text_field :brand, value: @item.brand, placeholder: "Enter new Brand" %><br>
  <label> Category </label><br>
  <%= f.text_field :category, value: @item.category, placeholder: "Enter new Category" %><br>
  <%= image_tag @item.image.url(:large) %>
  <%= f.file_field :image %><br>
  <%= f.submit "Update Item" %>
<% end %>
<br><br><br><br>
<%= button_to "Delete Item", { action: "destroy", id: @item.id }, method: :delete %>

<!-- TODO: the views code could be a bit cleaner.. let's work on better indentation! -->
<!-- TODO: I would add flash messaging to let people know if they successfully did something or when there are errors.. I.e. when I put in pant.. nothing is added, or when I put in tags with commas.. it doesn't work, etc. -->
